int sgn(float n)
{
	if (n < 0)
		return (-1);
	else
		return (1);
	return (0);
}

int	min(int a, int b)
{
	if (a < b)
		return (a);
	return (b);
}

int	max(int a, int b)
{
	if (a > b)
		return (a);
	return (b);
}
